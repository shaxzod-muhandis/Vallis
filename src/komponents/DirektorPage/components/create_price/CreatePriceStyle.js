import styled from "styled-components";

const CreatePrice = styled.div`
  td:first-child,
  th:first-child {
    border-left: none;
  }
  tr:last-child {
    border-bottom: none;
  }
`;

export default CreatePrice;
